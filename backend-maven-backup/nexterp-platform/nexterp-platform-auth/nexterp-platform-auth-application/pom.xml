<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.nexterp</groupId>
        <artifactId>nexterp-platform-auth</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>nexterp-platform-auth-application</artifactId>

    <name>NextERP Platform Auth Application</name>
    <description>NextERP 平台认证授权模块 - 应用层</description>

    <dependencies>
        <!-- Auth Domain -->
        <dependency>
            <groupId>com.nexterp</groupId>
            <artifactId>nexterp-platform-auth-domain</artifactId>
        </dependency>

        <!-- Auth API (for DTOs) -->
        <dependency>
            <groupId>com.nexterp</groupId>
            <artifactId>nexterp-platform-auth-api</artifactId>
        </dependency>

        <!-- Auth Infrastructure Core (for security classes) -->
        <dependency>
            <groupId>com.nexterp</groupId>
            <artifactId>nexterp-platform-auth-infrastructure-core</artifactId>
        </dependency>

        <!-- Auth Infrastructure Handlers (for authentication handlers) -->
        <dependency>
            <groupId>com.nexterp</groupId>
            <artifactId>nexterp-platform-auth-infrastructure-handlers</artifactId>
        </dependency>

        <!-- Shared Core -->
        <dependency>
            <groupId>com.nexterp</groupId>
            <artifactId>nexterp-shared-core</artifactId>
        </dependency>

        <!-- Shared Security -->
        <dependency>
            <groupId>com.nexterp</groupId>
            <artifactId>nexterp-shared-security</artifactId>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test Dependencies -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
