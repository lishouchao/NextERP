apiVersion: v1
kind: Service
metadata:
  name: nexterp-frontend
  namespace: nexterp
  labels:
    app: nexterp-frontend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: nexterp-frontend
